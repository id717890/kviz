export default async ({ store, redirect }) => {
  // if (!store?.quiz?.id) {
  //   // const errorMessage = 'QUIZ ID NOT FOUND'
  //   // console.log(errorMessage)
  //   redirect({ name: 'error', params: { message: 123 } })
  // }
  console.log('check ID')
}
